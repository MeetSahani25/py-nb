name: Daily Screener Report

on:
  schedule:
    - cron: "30 12 * * 1-5"   # 6pm IST Mon-Fri — daily scrape
  workflow_dispatch:

jobs:
  scrape:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install requests beautifulsoup4 yfinance

      - name: Run daily scraper (Mon-Fri only)
        run: |
          DAY=$(date +'%u')
          EVENT="${{ github.event_name }}"
          if [ "$DAY" -le "5" ] || [ "$EVENT" = "workflow_dispatch" ]; then
            echo "Running daily scraper (day=$DAY)..."
            python screener_scraper.py
          else
            echo "Saturday — skipping daily scrape"
          fi
        env:
          SCREENER_EMAIL: ${{ secrets.SCREENER_EMAIL }}
          SCREENER_PASSWORD: ${{ secrets.SCREENER_PASSWORD }}

      - name: Run weekly analysis (Fridays + manual only)
        run: |
          DAY=$(date +'%u')
          EVENT="${{ github.event_name }}"
          if [ "$DAY" = "5" ] || [ "$EVENT" = "workflow_dispatch" ]; then
            echo "Running weekly analysis (day=$DAY)..."
            python weekly_analysis.py
          else
            echo "Not Friday — skipping weekly analysis (day=$DAY)"
          fi

      - name: Run silent horse analysis (Fridays + manual only)
        run: |
          DAY=$(date +'%u')
          EVENT="${{ github.event_name }}"
          if [ "$DAY" = "5" ] || [ "$EVENT" = "workflow_dispatch" ]; then
            echo "Running silent horse 3-week + 4-week..."
            python silent_horse.py
          else
            echo "Not Friday — skipping silent horse (day=$DAY)"
          fi

      - name: Run cleanup (Fridays only)
        run: |
          if [ "$(date +'%u')" = "5" ]; then
            echo "Running 180-day cleanup..."
            python cleanup.py
          fi

      - name: Send email (Fridays + Saturdays + manual)
        run: |
          DAY=$(date +'%u')
          EVENT="${{ github.event_name }}"
          if [ "$DAY" = "5" ] || [ "$DAY" = "6" ] || [ "$EVENT" = "workflow_dispatch" ]; then
            echo "Sending email..."
            python send_email.py
          else
            echo "Not email day — skipping"
          fi
        env:
          EMAIL_SENDER: ${{ secrets.EMAIL_SENDER }}
          EMAIL_PASSWORD: ${{ secrets.EMAIL_PASSWORD }}
          EMAIL_RECEIVER: ${{ secrets.EMAIL_RECEIVER }}

      - name: Commit and push
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add reports/
          git diff --cached --quiet || git commit -m "📈 $(date +'%Y-%m-%d')$([ $(date +'%u') -eq 5 ] && echo ' + weekly + silent horse' || echo '')"
          git push
